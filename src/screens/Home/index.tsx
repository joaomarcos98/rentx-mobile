import React from 'react';

import * as Styled from './styles';

export const Home = () => {
    return (
        <Styled.Container>
            <Styled.Title>
                HOME
            </Styled.Title>

        </Styled.Container>
    );
}